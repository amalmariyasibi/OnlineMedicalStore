{
  "name": "online-medical-store",
  "version": "1.0.0",
  "description": "Online Medical Store - Monorepo with frontend and backend",
  "scripts": {
    "build": "cd online_frontend && npm install && npm run build",
    "start": "cd online_frontend && npm start",
    "test": "cd online_frontend && npm test"
  }
}
